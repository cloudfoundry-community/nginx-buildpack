task :cleanup { sh "support/heroku-buildpack cleanup" }
task :build { sh "support/heroku-buildpack build" }
task :setup { sh "support/heroku-buildpack setup" }
task :download { sh "support/heroku-buildpack download"}
